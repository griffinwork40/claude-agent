#!/usr/bin/env bash
# .husky/pre-commit Purpose: Run project linting and type-check tasks before commits.

set -eu

echo "🔍 Running linter..."
npm run lint
echo "🔧 Running type check..."
npx tsc --noEmit
