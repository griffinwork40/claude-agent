# job-apply-agent

Promoted Next.js app from `frontend/` to repo root.

## Develop

1. Copy `.env.example` to `.env.local` and set values.
2. Install deps: `npm install`
3. Run dev: `npm run dev`

## Build

`npm run build` then `npm start`.
